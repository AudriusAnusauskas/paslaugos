import { DiscCodDirective } from './disc-cod.directive';

describe('DiscCodDirective', () => {
  it('should create an instance', () => {
    const directive = new DiscCodDirective();
    expect(directive).toBeTruthy();
  });
});
